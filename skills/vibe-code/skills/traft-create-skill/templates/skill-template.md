---
name: skill-name(与文件夹名称保持一致)
description: 技能简述 + 触发场景/关键词（路由只靠这段文字判断是否选它，写得越具体越不易误触发，控制字数不要太多）
---

# xxx skill
技能简述，明确**职责边界**（明确「负责/不负责」，防止技能被错误调用;超出边界时直接说明并转交，不硬接。）

## 1. 触发条件（TRIGGER）
*说明:用表格列出场景与触发信号。正例至少 3 条、反例至少 1 条——反例最能防误触发。*

| 场景 | 触发信号(关键词/典型句式) | 是否触发 |
| --- | --- | --- |
| 示例:分析报错 | "排查下这个报错"、"这个异常从哪来的" | 触发 |
| 示例:闲聊/泛泛提问 | "随便聊聊"、"你怎么看" | 不触发,交给默认回复 |

## 2. 规则（RULES）

### 2.1 务必执行(ALWAYS DO)
*说明:写「不遵守就会出错」的行为，2~5 条足够。*
1. xxx
2. xxx

### 2.2 禁止执行(NEVER DO)
*说明:写会带来破坏或误导的行为，尤其是容易犯的错。*
1. xxx
2. xxx

### 2.3 条件执行(CONDITIONAL)
*说明: 特殊条件下，需要做什么，不能做什么。
- 需要的输入:xxx(如日志路径、PR 链接、命令输出)
- 缺失时:xxx(如实告知缺失项，不臆造、不猜测)

## 3. 知识底座（CONTEXT）
*说明:技能做判断的依据——项目约定、技术栈、常用脚本、目录结构、相关文档链接。约定变更时同步更新本节。*

- xxx

## 4. 工作流程（WORKFLOW）
*说明:每一步写清「做什么 + 完成标志」，分支用 IF/ELSE。完成标志不满足则不许进入下一步。*

### Step 1: xxx
- 动作:xxx
- 完成标志:xxx

### Step 2: xxx
- IF <条件> → 走 A:xxx
- ELSE → 走 B:xxx
- 完成标志:xxx

### Step 3: xxx
- 动作:xxx
- 完成标志:xxx

## 5. 输出规范（OUTPUT SPEC）
*说明:固定输出格式，分析类问题必需，工具类技能可选。*

### 5.1 固定格式
```text
## 失败根因
<一句话结论: 属于哪类问题(数据/参数/环境/代码/依赖), 最直接的原因是什么>
## 证据链
- 异常类型: <ExcType>
- 触发点: `<仓库文件:行号>` 的 `<函数>`, 此处 `raise <ExcType>`
- 底层根因: <底层根因行原文>
- 关键代码: <引用 raise 帧/最深仓库帧附近的关键代码>
## 解决方案
1. <最可能的修复>
2. <备选或需进一步确认的方向>
```

### 5.2 字段要求
*说明:解释每个字段的写作标准，避免填出空话。*

| 字段   | 写作要求                      |
| ---- | ------------------------- |
| 失败根因 | 一句话，先结论后原因，落在问题类别上        |
| 证据链  | 每条都可追溯到文件:行号/日志/异常类型，不给猜测 |
| 解决方案 | 第一条是最可能的修复，其余列为备选方向       |

### 5.3 交付前自检
- [ ] 直接回答了用户的问题，没有跑偏
- [ ] 证据可追溯到具体文件/行号/日志，没有臆造
- [ ] 给出了可执行的下一步或明确结论

## 6. 示例（可选，强烈推荐）
*说明:一组「好的输入输出」与「差的输入输出」，是对齐模型行为最有效的方式。*

### 6.1 好的示例
**输入**:xxx
**输出**:xxx

### 6.2 差的示例
**输入**:xxx
**输出**:xxx


